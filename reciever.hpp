#include <string>

#if !defined(RECIEVER)
#define RECIEVER
class Reciever
{
public:
    void faca_algo(const std::string &a);
    void faca_algo_diferente(const std::string &b);
};

#endif // RECIEVER